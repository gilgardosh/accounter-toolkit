{
  "name": "accounter-toolkit",
  "type": "module",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "18.15.0"
  },
  "scripts": {
    "build": "bob build",
    "ci:lint": "eslint --output-file eslint_report.json --format json",
    "lint": "eslint --ignore-path .gitignore .",
    "mesh:artifacts-rename": "yarn ts-node ./helpers/mesh-artifacts-rename.mjs",
    "postinstall": "patch-package",
    "pre-commit": "lint-staged",
    "prebuild": "rimraf dist/ .bob/ && yarn lint && yarn prettier && yarn workspaces run prebuild && yarn mesh:artifacts-rename",
    "prettier": "prettier --ignore-path .prettierignore --write --list-different .",
    "prettier:check": "prettier --check .",
    "release": "yarn build && changeset publish"
  },
  "dependencies": {
    "@changesets/changelog-github": "0.4.8",
    "@changesets/cli": "2.26.0",
    "glob": "9.2.1"
  },
  "devDependencies": {
    "@theguild/eslint-config": "0.8.0",
    "@theguild/prettier-config": "2.0.0",
    "@types/node": "18.15.1",
    "bob-the-bundler": "5.0.1",
    "concurrently": "7.6.0",
    "eslint": "8.36.0",
    "husky": "8.0.3",
    "lint-staged": "13.2.0",
    "patch-package": "^6.5.1",
    "prettier": "3.0.0",
    "prettier-plugin-sql": "0.13.0",
    "typescript": "4.9.5"
  },
  "resolutions": {
    "**/execa": "5.1.1",
    "lint-staged/execa": "6.1.0"
  },
  "publishConfig": {
    "directory": "dist",
    "access": "public"
  },
  "lint-staged": {
    "*.--write": "prettier --write"
  }
}
